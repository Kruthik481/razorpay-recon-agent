"""A local review console: the exception queue, with buttons.

Runs on the standard library alone and binds to the loopback interface only.
Its whole purpose is to close the learning loop where somebody can watch it:
confirm a few exceptions, promote them, and see the queue shrink.
"""
